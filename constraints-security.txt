# Minimums required by the supplied 2026-09-14 security audit.
# Compatibility must pass the clean Linux/Python resolver + full release gates.
# The build environment could not retrieve these packages; no exact lock or
# successful clean compatibility resolution is claimed. Do not relax on failure.
aiohttp>=3.14.3,<4
urllib3>=2.7.0,<3
idna>=3.15,<4
python-dotenv>=1.2.2,<2
